#ifndef ADVENT
#define ADVENT

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef long long I;
typedef double D;
typedef char C;

#define R return
#define DO(n,x) {I i=0,_i=(n);for(;i<_i;++i){x;}}
#define MAX(a,w) (a>w?a:w)
#define MIN(a,w) (a<w?a:w)

#endif
